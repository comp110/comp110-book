[project]
name = "nextgen"
version = "0.1.0"
description = "A Zensical-based static website."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "playwright>=1.60.0",
  "pytest>=9.1.0",
  "zensical==0.0.45",
]

[tool.uv]
package = false
